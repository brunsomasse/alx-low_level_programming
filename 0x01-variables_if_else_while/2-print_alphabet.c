#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 *main - prints the alphabet in lowercase, followed by a new line
 * 
 *Return: Always 0 (Success)
 */
int main(void)
{
int n;
int char;
for (ch = 97; ch <= 122; ch++)
{
putchar(ch);
}
putchar("\n");
return (0);
}
