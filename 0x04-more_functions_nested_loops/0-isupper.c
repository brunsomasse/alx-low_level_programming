#include "main.h"

/**
 * _isupper - a function that checks for uppercase character
 * @c: an integer parameter
 * Return: Success
 */

int _isupper(int c)

{
if (c >= 'A' && c <= 'Z)
return (1);
return (0);
}
